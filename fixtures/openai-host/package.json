{
  "name": "cometapi-openai-host-fixture",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm run typecheck && node verify.mjs && node verify.cjs",
    "typecheck": "tsc --noEmit --project tsconfig.json && tsc --noEmit --project tsconfig.nonexact.json"
  },
  "dependencies": {
    "openai": "6.47.0"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
